<?xml version="1.0" encoding="utf-8"?>
<custom:CustomWindow x:Class="S9BEditor.TypeSelectionWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:custom="clr-namespace:S9BEditor" Style="{StaticResource BPEWindowStyle}" Height="384" Width="343" CanMaximize="False" CanMinimize="False" WindowStartupLocation="CenterOwner" MinWidth="211" MinHeight="143" HasDialogBackground="True" Title="{Binding Source={StaticResource appTitle}}" DataContext="{Binding RelativeSource={RelativeSource Self}}">
  <Grid>
    <Button Name="button2" Content="Cancel" Height="23" HorizontalAlignment="Right" IsCancel="True" Margin="0,0,6,6" VerticalAlignment="Bottom" Width="75" />
    <Button Name="button3" Content="OK" Height="23" HorizontalAlignment="Right" IsDefault="True" Margin="0,0,87,6" VerticalAlignment="Bottom" Width="75" Click="button3_Click" />
    <ListBox Name="listBox1" Margin="6,40,6,35" ItemsSource="{Binding ItemsSource}" DisplayMemberPath="{Binding DisplayMemberPath}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" />
    <Label Name="label1" Content="Select a data type:" Height="28" HorizontalAlignment="Left" Margin="6,6,0,0" VerticalAlignment="Top" />
  </Grid>
</custom:CustomWindow>