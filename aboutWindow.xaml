<?xml version="1.0" encoding="utf-8"?>
<custom:CustomWindow x:Class="S9BEditor.AboutWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:custom="clr-namespace:S9BEditor" Style="{StaticResource BPEWindowStyle}" CanMaximize="False" CanMinimize="False" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Title="About" Height="200" Width="400" DataContext="{Binding RelativeSource={RelativeSource Self}}">
  <Grid>
    <FrameworkElement.Resources>
      <ResourceDictionary>
        <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
          <Setter Property="Foreground" Value="#FFFFFFFF" />
        </Style>
      </ResourceDictionary>
    </FrameworkElement.Resources>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock Margin="10,8,10,2">
      <Run FontSize="20" Text="{StaticResource appTitle}" /> <Run Text="© 2014 Dovetail Games" /></TextBlock>
    <TextBlock Grid.Row="1" Margin="16,2">Version: <Run Text="{Binding Version, Mode=OneWay}" /></TextBlock>
    <Button Content="OK" Grid.Row="3" Margin="0,0,10,11" Height="23" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="75" IsDefault="True" IsCancel="True" />
  </Grid>
</custom:CustomWindow>