<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:custom="clr-namespace:S9BEditor">
  <Style x:Key="{x:Type custom:DirectoryTreeView}" TargetType="{x:Type custom:DirectoryTreeView}" BasedOn="{StaticResource BPETreeViewStyle}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type custom:DirectoryTreeView}">
          <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}">
            <ItemsPresenter />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type custom:DirectoryTreeViewItem}" TargetType="{x:Type custom:DirectoryTreeViewItem}" BasedOn="{StaticResource BPETreeViewItemStyle}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
    <Setter Property="Header" Value="{Binding ActualDisplayName, Mode=OneWay, RelativeSource={RelativeSource Self}}" />
  </Style>
</ResourceDictionary>