<?xml version="1.0" encoding="utf-8"?>
<custom:CustomWindow x:Class="S9BEditor.UnhandledExceptionDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:custom="clr-namespace:S9BEditor" Style="{StaticResource BPEWindowStyle}" CanMaximize="False" CanMinimize="False" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Title="Unhandled Exception" Height="372" Width="538" HasDialogBackground="True" DataContext="{Binding RelativeSource={RelativeSource Self}}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Image Margin="4,4,0,4" Source="Resources/error.png" MaxWidth="32" MaxHeight="32" />
    <TextBlock Grid.Column="1" Margin="4" TextWrapping="Wrap">Blueprint Editor 2 has encountered an unhandled exception. It may be safe to continue, but it is recommended that the program is restarted to prevent further issues.</TextBlock>
    <TextBlock Margin="4,0,4,0" Grid.Row="1" Grid.ColumnSpan="2">Details:</TextBlock>
    <TextBox Grid.Row="2" Grid.ColumnSpan="2" Margin="4" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Text="{Binding ExceptionText}" />
    <Button Name="button4" Grid.Row="3" Grid.ColumnSpan="2" Content="Continue" Height="23" HorizontalAlignment="Right" Margin="0,0,6,6" VerticalAlignment="Bottom" Width="75" Click="button3_Click" />
    <Button Name="button3" Grid.Row="3" Grid.ColumnSpan="2" Content="Exit" Height="23" HorizontalAlignment="Right" Margin="0,0,86,6" VerticalAlignment="Bottom" Width="75" IsDefault="True" Click="button4_Click" />
  </Grid>
</custom:CustomWindow>